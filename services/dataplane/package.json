{
  "name": "@shutters/dataplane-service",
  "private": true,
  "dependencies": {
    "@shutters/dataplane": "workspace:*"
  },
  "scripts": {
		"clean": "rimraf --no-interactive dist",
		"build": "bun build --compile --minify --sourcemap ./src/index.ts --outfile dist/dataplane",
		"build:clean": "bun run clean && bun run build",
    "dev": "bun run ./src/index.ts"
  }
}
